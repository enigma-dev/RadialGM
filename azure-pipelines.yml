pr:  # Trigger a build on every PR.
  branches:
    include:
      - '*'  # Must quote since "*" is a YAML reserved character
      
jobs:
- template: azure-jobs.yml  # Template reference
  parameters:
    submodule: true
